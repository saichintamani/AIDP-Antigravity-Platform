[pytest]
markers =
    integration: mark test to run as integration test (requires external API/network)
    asyncio: mark test to run async
    flagship: mark test as part of the Phase 22 flagship validation suite
