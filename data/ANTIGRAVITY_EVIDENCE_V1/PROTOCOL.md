# ANTIGRAVITY_EVIDENCE_V1

This directory is the empirical center of the Antigravity OS project, provisioned under **Directive 008**.

## Protocol
1. **Raw Storage Only:** The `.json` files in this directory are strictly for storing raw outputs from 5+ human evaluators (AlignEval) and 3+ frontier models (ConstraintBench).
2. **No Early Calculation:** Success metrics (Precision, Recall, F1) are intentionally blocked from being calculated until data collection is complete.
3. **Preserve Anomalies:** Every disagreement between humans, and every unexpected LLM hallucination, will be preserved exactly as generated.

Awaiting data insertion.
